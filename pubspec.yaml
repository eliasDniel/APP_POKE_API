name: flutter_poke_api
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  animate_do: ^4.2.0
  dio: ^5.9.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  flutter_staggered_grid_view: ^0.7.0
  flutter_svg: ^2.2.0
  go_router: ^16.2.0
  google_fonts: ^6.3.1
  salomon_bottom_bar: ^3.3.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/loaders/
    - assets/icons/
    - assets/images/
